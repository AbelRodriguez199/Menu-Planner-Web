/*import http from './BaseService';

const register = user => http.category('/register', user)

const authenticate = credentials => http.category(`/authenticate`, credentials)

const logout = () => http.category(`/logout`)

export default {
  register,
  authenticate,
  logout
}
*/